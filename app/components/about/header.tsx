import Image from "next/image";
import React from "react";

function Header() {
  return (
    <div className="bg-[var(--pre-primary)]/10 flex items-center justify-center"></div>
  );
}

export default Header;
